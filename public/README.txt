Place static assets here.

hero.mp4  — Optional background video for the homepage hero.
            Recommended: 1920x1080, H.264, muted, ~10–20s loop, < 5 MB.
            If absent, the hero falls back to the poster image automatically.

favicon / og images — add favicon.ico and opengraph-image.png to /app for
metadata, per Next.js conventions.
